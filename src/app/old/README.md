# Archived legacy public pages

These routes were the original marketing site before the v2 redesign.

They are kept for reference only and are served under `/old/*` (e.g. `/old/about`, `/old/booking`).

The live site uses the routes in `src/app/(site)/`.
